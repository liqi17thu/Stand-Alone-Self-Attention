# auto_resume: True
train:
  epoch: 180
  attention_epoch: 160
  disp: 10

model:
    name: 'PoolResNet50'
    stem: 'imagenet_conv'

dataset:
  name: 'imagenet'
  train_dir: './data/imagenet/train'
  test_dir: './data/imagenet/val'
  image_size: 224
  batch_size: 256

optim:
  sgd_params:
    lr: 0.15
